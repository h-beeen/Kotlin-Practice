## Kotlin Study

```kotlin
fun main() {
    val number = 10 // int형 변수 추론
    val language = "Korean" // String형 변수 추론
    
    val username: String = "Haebin";
    // 선언 키워드, 변수 이름, 자료형, 값
    // 값을 할당하지 않으면서 변수를 선언하려면, 자료형을 반드시 지정해야 한다.
    // IntelliJ에서 `Ctrl + Shift + P` 를 누르면 추론된 자료형 확인 가능
    
    // 코틀린에서는 Primitive Type을 사용할 필요가 없다. -> 코틀린 컴파일러가 참조형으로 변경
}
```
